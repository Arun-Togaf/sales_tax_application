package com.tax.app.model;

/**
 * enum class representing the item category
 * New item categories can be added if required
 * Others category is used to denote items other than Book, Food and Medicine
 *
 */
public enum ItemCategory {
	BOOK, FOOD, MEDICINE, OTHERS
}
